import ContactClient from "./ContactClient";

export const metadata = {
  title: "Contact Us | Farsan Gold",
  description:
    "Contact Farsan Gold for fresh farsan snacks, bulk orders, franchise inquiries and customer support.",
};

export default function ContactPage() {
  return <ContactClient />;
}
